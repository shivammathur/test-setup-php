name: Runner workflow
on: [push]
jobs:
  testing:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: 7.4
          tools: deployer:6.x
      - run: deployer -V    
      

    
