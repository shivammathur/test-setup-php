name: Runner tests

on: push

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix: 
        os: [ubuntu-18.04, ubuntu-20.04]
    steps:
        - uses: mxschmitt/action-tmate@v3.11
