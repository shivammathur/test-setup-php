name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest]
    runs-on: ${{matrix.os}}
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: '8.5'
      - run: php -v
