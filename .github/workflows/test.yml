name: runner

on:
  push:

jobs:
  build:
    strategy:
      matrix:
        container: [alpine, '']
    runs-on: ubuntu-latest
    container: ${{ matrix.container }}
    steps:
      - run: cat /etc/os-release
