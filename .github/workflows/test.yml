
name: Runner
on:
  push:
jobs:
  run:
    runs-on: macos-15
    steps:
      - run: brew config
      - run: sudo xcode-select -s /Applications/Xcode_26.1.1.app
      - uses: mxschmitt/action-tmate@v3.23
      - run: brew config
      - run: |
          brew tap shivammathur/php
          brew install -svd php@8.6
