  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: Test
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [ubuntu-16.04, ubuntu-18.04, ubuntu-20.04]
      fail-fast: false
    steps:
      - run: |
          sudo apt-cache policy libzip[0-9]
          sudo apt-get install libzip[0-9]
