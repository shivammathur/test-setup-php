name: runner

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 8.4
          extensions: redis-6.0.1
      - name: Test
        run: |
          php -r "echo phpversion('redis');"
          php -v
          php -m
