name: Test
on:
  push:
jobs:
  run:
    runs-on: windows-latest
    steps:
      - name: Test
        run: |
          dir "C:\Program Files (x86)\Microsoft Visual Studio\Installer\"
                 
               
