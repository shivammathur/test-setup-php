name: 'Runner Test'
on: [push]
jobs:
  run:
    strategy:
      matrix:
        os: [ubuntu-24.04, ubuntu-22.04, ubuntu-latest, ubuntu-slim]
    runs-on: ${{ matrix.os }}
    name: Test
    steps:
    - run: cat /imagegeneration/imagedata.json
