name: Test

on:
  push:

jobs:
  build:
    name: Test
    runs-on: ubuntu-18.04
    steps:
    - uses: shivammathur/setup-php@develop
      with:
        php-version: 5.6
    - run: |    
        php -v
        php -m
    - uses: mxschmitt/action-tmate@v3.3
