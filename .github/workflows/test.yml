
name: Runner Workflow
on:
  push:
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
    steps:
      - run: |
          $cpuInfo = Get-CimInstance Win32_Processor
          $cpuInfo | $_.ExtensionData.InstructionSet

