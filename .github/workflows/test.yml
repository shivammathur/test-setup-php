name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [ubuntu-latest]        
    runs-on: ${{ matrix.os }}
    steps:
      - uses: shivammathur/setup-php@develop
        with:
          php-version: 8.0
      - name: Print default
        run: |
          echo '{ }' > composer.json
          composer config process-timeout
          
