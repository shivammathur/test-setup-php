  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
         os: [windows-latest]
    steps:
      - name: Test
        run: |
          New-Item -Path test.ps1 -Value "echo test"
          Measure-Command { powershell test.ps1 }
          Measure-Command { pwsh test.ps1 }
