name: Runner workflow
on: [push]
jobs:
  run:
    name: Run
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-20.04, macos-latest]
#       node: [8, 10, 12, 14]
    steps:
    - run: echo 'echo "welcome"' | sudo tee -a $HOME/.bashrc
    - run: echo 'Test'
