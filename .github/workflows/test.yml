name: HTTP workflow
on:
  push:
jobs:
  php-stan:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@more-verbose
        with:
          php-version: 8.3
          extensions: gnupg
      - run: php -v && php -m
