name: Build and Test on Windows
on: [push]
jobs:
  windows:
    defaults:
      run:
        shell: cmd
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2022]
    runs-on: ${{matrix.os}}
    steps:
      - uses: mxschmitt/action-tmate@v3
