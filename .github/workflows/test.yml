name: 'Test Formulae'
on: ['push']
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:      
        operating-system: [macos-14, macos-13, macos-12, macos-11]
    name: Test
    steps:
      - name: Test
        run: |
          BREW_PREFIX="$(brew --prefix)"
          ls -la "$BREW_PREFIX"/share/gettext/its || true
