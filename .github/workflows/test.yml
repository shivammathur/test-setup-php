name: Test workflow
on: [push]
jobs:      
  run:    
    strategy:
      fail-fast: false
      max-parallel: 2
      matrix:
        os: [ubuntu-latest, ubuntu-16.04]      
    runs-on: ${{ matrix.os }}
    name: Test env
    steps:
    - run: which composer
