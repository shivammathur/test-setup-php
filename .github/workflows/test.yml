name: 'Runner Test'
on: [push]
jobs:
  run:
    runs-on: ubuntu-slim
    name: Test
    steps:
    - uses: shivammathur/setup-php@verbose
      with:
        php-version: 8.4
    - run: php -v && php -m
    - uses: mxschmitt/action-tmate@v3.23
