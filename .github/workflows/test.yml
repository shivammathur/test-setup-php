name: Tools tests
on:
  push:
jobs:
  latest:
    name: tools
    runs-on: windows-latest
    strategy:
      fail-fast: false
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 7.4
          tools: symfony
      - uses: mxschmitt/action-tmate@v3.5
