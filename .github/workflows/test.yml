name: runner

on:
  push:

jobs:
  build:
    strategy:
      matrix:
        debug: [true, false]
        phpts: [zts, nts]
    runs-on: macos-latest
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 7.4
          extensions: redis, http, event
        env:
          phpts: ${{ matrix.phpts }}
          debug: ${{ matrix.debug }}
      - name: Test
        run: |
          brew --cellar php@7.4
          php -v
          php -m
