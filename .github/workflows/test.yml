on:
  - push

name: build

jobs:
  tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - windows-2016
          - windows-latest
    steps:
    - run: |
        Get-ChildItem "C:\Program Files\7-Zip"
        try { 7z -mqs } catch { }
          
