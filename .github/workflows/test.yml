name: Runner workflow
on: [push]
jobs:
  run:
    name: 'Test'
    runs-on: 'ubuntu-24.04'
    
    steps:
        - run: |
            apt-cache policy msodbcsql17
            apt-cache policy unixodbc
        
