name: Runner test
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: 8.1

      - run: php -v && php -m    
