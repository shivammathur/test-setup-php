name: Test #test
on:
  - push
jobs:
  build:
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
    steps:
    - name: Test
      run: sudo apt-get install -y apt-fast automake gcc git jq make pkg-config shtool snmp sudo unzip
