name: Test workflow
on: [push]
jobs:
  test:    
    name: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Test exit 
        run: |
          cross="✗"
          export version='5.3'
          export runner='self-hosted'
          curl -OSL https://raw.githubusercontent.com/shivammathur/setup-php/develop/src/scripts/linux.sh
          sed -i "s/sleep 5/set -e/g" linux.sh
          cat linux.sh
          bash linux.sh $version $(pwd)

      - name: Should not run
        run: echo 'test'
        
          
