name: Test #test
on:
  - push
jobs:
  build:
    runs-on: ubuntu-20.04
    strategy:
      fail-fast: false
    steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '7.2'
    - run: |
        sudo apt-get update
        sudo apt-get install php7.2-imagick -y
