name: Runner
on:
  push:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 8.6
      - run: |
          php -v
        
