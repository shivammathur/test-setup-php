name: 'Test Formulae'
on: [push]
jobs:
  tests:
    strategy:
      matrix:
        os: [ubuntu-22.04]
    runs-on: ${{ matrix.os }}
    steps:
        - run: |
            echo $RUNNER_TEMP
            echo $GITHUB_OUTPUT
            
