name: Runner tests

on: push

jobs:
  test:
    container: ubuntu:21.10
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '7.4|8.0'
