name: 'Test'
on:
  push:
jobs:
  run:
    strategy:
      matrix:
        os: [ubuntu-latest]
        php: ['8.0']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: ${{ matrix.php }}
      - uses: mxschmitt/action-tmate@v3.3
