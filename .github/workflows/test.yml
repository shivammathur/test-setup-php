name: Runner
on:
  push:
jobs:  
    runner:
      runs-on: macos-14
      steps:
        - uses: shivammathur/setup-php@verbose
          with:
            php-version: 8.5
        - run: php -v && php -m
