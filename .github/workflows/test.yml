name: Runner workflow
on: [push]
jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [windows-latest]
    name: Test on ${{ matrix.os}}
    steps:
    - run: |
        python --version
        gci env:
       
