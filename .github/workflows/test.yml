name: Main workflow
on: [push]
jobs:
  run:
    strategy:
      matrix:
        os: [ubuntu-20.04, ubuntu-22.04, ubuntu-24.04]
    name: Run
    runs-on: ${{ matrix.os }}
    steps:
      - run: sudo apt-cache policy libraqm*
