name: Runner
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    container: homebrew/ubuntu22.04:latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-22.04-arm]
    steps:
      - run: arch
