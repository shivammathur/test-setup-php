name: 'Test'
on:
  push:
jobs:
  run:
    strategy:
      matrix:
        os: [macos-latest]
        php: ['8.1']
    runs-on: ${{ matrix.os }}
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: ${{ matrix.php }}
