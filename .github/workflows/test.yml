  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: Test
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Test
        run: |
          sudo curl -sL https://raw.githubusercontent.com/ilikenwf/apt-fast/master/apt-fast -o /usr/local/bin/apt-fast
          sudo chmod +x /usr/local/bin/apt-fast
