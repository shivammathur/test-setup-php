name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [macos-latest]        
    runs-on: ${{ matrix.os }}
    steps:
      - run: brew tap shivammathur/php && brew install php@8.1 && brew link --force --overwrite php@8.1
      - run: php -v
          
