name: Test
on:
  push:
jobs:
  runner:
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04, ubuntu-latest]
    name: Test
    runs-on: ${{ matrix.os }}
    steps:
      - run: |
          ping google.com -c 2
          ping getcomposer.org -c 2
