name: Test workflow
on: [push]
jobs:
  run:    
    runs-on: macos-latest
    steps:
    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: '8.0'
        extensions: http
    - uses: mxschmitt/action-tmate@v3.5
      
