name: Test
on:
  push:
jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-20.04, macos-10.15]
    steps:
      - name: Test
        run: |
          echo $GITHUB_ENV || true
          echo $GITHUB_PATH || true
          cat $GITHUB_ENV || true
          cat $GITHUB_PATH || true
