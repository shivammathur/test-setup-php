name: Test
on:
  push:
jobs:
  run:
    runs-on: ${{ matrix.os }}
    container: ubuntu:precise
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
    - name: Test
      run: |
        apt-get update
        apt-get install lsb-release -y
        lsb_release -a
        

          
