name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:         
      - name: Test
        run: |
          (
            cd /tmp
            sudo mkdir -p /usr/lib/jvm
            curl -o zulu6.tar.gz -sL https://cdn.azul.com/zulu/bin/zulu6.22.0.3-jdk6.0.119-linux_x64.tar.gz            
            tar -xzf zulu6.tar.gz -C /usr/lib/jvm
            export JAVA_HOME=/usr/lib/jvm/zulu6.22.0.3-jdk6.0.119-linux_x64/
            echo "::set-env name=JAVA_HOME::/usr/lib/jvm/zulu6.22.0.3-jdk6.0.119-linux_x64/"
            
            curl -o maven.gz -sL https://repo.maven.apache.org/maven2/org/apache/maven/apache-maven/3.2.5/apache-maven-3.2.5-bin.tar.gz
            tar -xzf maven.gz -C /tmp
            sudo cp /tmp/apache-maven-3.2.5/* /usr/
          )
          /usr/lib/jvm/zulu6.22.0.3-jdk6.0.119-linux_x64/bin/java -version          
          java -version
          mvn -v
        
