name: Runner
on: [push]
jobs:
  one:
    name: Run
    runs-on: ${{ matrix.os }}    
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest]
    steps:
      - uses: mxschmitt/action-tmate@v3.23
