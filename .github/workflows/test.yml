name: Test runner workflow #
on: [push]
jobs:
  run:
    runs-on: ${{ matrix.operating-systems }}
    strategy:
      fail-fast: false
      matrix:        
        operating-systems: [ubuntu-latest]
    steps:
      - run: echo '::warning file=CareLineLive/app/Http/Controllers/Exports/CarersExportController.php,line=::Found violation(s) of type no_unused_imports'
      - uses: mxschmitt/action-tmate@v3.7
