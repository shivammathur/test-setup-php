name: Runner
on: [push]
jobs:
  e2e-tests:
    name: Run
    runs-on: ${{ matrix.os }}    
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, windows-2022, windows-2025, windows-2025-vs2026]
    steps:
      - run: php -v && php -m
