name: runner

on:
  push:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 8.1
          extensions: msgpack
      - name: Test
        run: |
          php -v
          php -m
