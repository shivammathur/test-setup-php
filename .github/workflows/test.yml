name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: Test
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [macos-latest]
      fail-fast: false
    steps:
      - name: Set
        id: build
        run: echo "::set-output name=build::true"
      - name: Test
        if: if ${{ steps.build.outputs.build }} == 'true'
        run: echo 'test'
