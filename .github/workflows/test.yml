name: Runner
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    container: homebrew/ubuntu22.04:latest
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, ubuntu-latest-arm]
    steps:
      - run: arch
