name: Develop tools workflow
on:
  repository_dispatch:
  push:
jobs:
  run:
    name: Run
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest, macOS-latest]
        php-versions: ['7.4']
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: ${{ matrix.php-versions }}
          tools: cs2pr
      - name: Patch cs2pr
        run: sed -i 's/\r$//' /usr/local/bin/cs2pr || sed -i '' 's/\r$//' /usr/local/bin/cs2pr
      - name: Testing Tools
        run: cs2pr version
