name: Test
on:
  push:
jobs:
  runner:
    strategy:
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04, ubuntu-latest]
    name: Test
    runs-on: ${{ matrix.os }}
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: 8.1
          tools: php-cs-fixer
