name: Runner workflow
on: [push]
jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-24.04]
    name: Test on ${{ matrix.os}}
    steps:
      - run: |
          sudo apt-get update
          sudo apt-get upgrade -y
