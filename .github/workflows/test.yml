name: Test workflow on windows
on: [push]
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:    
      matrix:
        operating-system: [windows-latest, windows-2016]
    name: Test windows env
    steps: 
    - name: Print env
      run: |
        printf $env:runner_toolcache
        printf $env:temp
        
