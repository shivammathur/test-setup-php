name: Runner
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: "Install PHP"
        uses: "shivammathur/setup-php@develop"
      - run: php -v  
