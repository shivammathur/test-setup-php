name: 'Test Formulae'
on: ['push']
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:      
        operating-system: [ubuntu-22.04, ubuntu-20.04, windows-2019, windows-2022, windows-2025, macos-14, macos-13, macos-12]
    name: Test
    steps:
    - run: php -v
