name: Tools tests
on:
  push:
jobs:
  phive:
    name: tools
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-20.04, windows-latest, macOS-latest]
        rel: [develop, v2]
    steps:
      - name: Install PHP
        uses: shivammathur/setup-php@${{ matrix.rel }}
        with:
          php-version: 8.0
          tools: php-cs-fixer
      - name: Testing Tools
        run: php-cs-fixer -V
