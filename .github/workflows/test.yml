name: 'Runner Test'
on: [push]
jobs:
  run:
    strategy:
      matrix: 
        os: [24.04, 22.04, slim, latest]
    runs-on: ubuntu-${{ matrix.os }}
    name: Test
    steps:
    - run: |
        echo $ImageOS
        echo $ImageVersion
        echo $RUNNER_ENVIRONMENT
        echo $CONTAINER
