name: Runner
on: [push]
jobs:
  one:
    name: Run
    runs-on: ${{ matrix.os }}    
    strategy:
      fail-fast: false
      matrix:
        os: [macos-26-intel, macos-15-intel]
    steps:
      - run: php -v
