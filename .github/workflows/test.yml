
name: Runner
on:
  push:
jobs:
  run:
    runs-on: ubuntu-slim
    strategy:
      matrix:
        php: [8.0, 8.1, 8.2, 8.3, 8.4, 8.5]
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php }}
          coverage: xdebug
      - run: php -v    
