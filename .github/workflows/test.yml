name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [macos-latest]        
    runs-on: ${{ matrix.os }}
    steps:
      - run: git -C "$(brew --repository)" log
      - run: git -C "$(brew --repository)" pull origin master
          
