name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2025]
    runs-on: ${{matrix.os}}
    steps:
      - name: shivammathur/setup-php@v2
        with:
          php-version: 8.4
        env:
          PHPTS: ts
      - run: ls C:\tools\php\
      
