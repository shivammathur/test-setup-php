name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest]
    name: Test
    runs-on: ${{ matrix.operating-system }}
    steps:
    - uses: shivammathur/setup-php@v2
      with:
        php-version: 8.3
        ini-file: development
        coverage: none
        tools: none
      env:
        phpts: ts
        debug: true
    - run: |
        php -v
        file /usr/lib/libphp8.3.so
        objdump --syms /usr/lib/libphp8.3.so | grep debug || true
