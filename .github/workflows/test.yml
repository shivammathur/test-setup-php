name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [windows-2019, windows-2016]
    runs-on: ${{ matrix.os }}
    steps:
      - run: Get-InstalledModule Pester              
          
