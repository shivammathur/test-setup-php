  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: Test
    runs-on: macos-latest
    strategy:
      fail-fast: false
    steps:
      - name: Test
        run: |
          run=$(echo 'buils-php-8.1' | grep -Eo 'build-php-[0-9].[0-9]' | cut -d '-' -f 3)
          echo $run
