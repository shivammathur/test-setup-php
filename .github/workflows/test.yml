name: Test workflow #Test
on: [push]
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:      
        operating-system: [ubuntu-latest, ubuntu-16.04]        
    name: Test on ${{ matrix.operating-system }}
    steps:
    - name: Test services
      run: ls /etc/init.d
    - name: service postgresql status
