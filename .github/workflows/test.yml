name: workflow.yml
on:
  push:

jobs:
  test:
    runs-on: macos-latest
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: 8.5
          extensions: xdebug
        env:
          update: true
      - run: php -v && php -m && composer -V
