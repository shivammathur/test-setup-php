on: [push]
name: test
jobs:
  test1:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-latest]
    steps:
    - run: |
        dep='libomp'
        brew fetch "$dep"
        echo "--------------------"
        ls "$(brew --cache)/downloads/*--$dep*.tar.gz"
        
