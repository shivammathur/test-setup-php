name: Runner
on:
  push:
jobs:
  repro:
    runs-on: ubuntu-latest
    steps:
      - name: Setup PHP with affected Composer
        uses: shivammathur/setup-php@develop
        with:
          php-version: 8.5
          tools: composer:2.9.7

      - name:
        run: composer -V
        
