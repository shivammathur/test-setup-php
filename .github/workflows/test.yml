name: Runner
on:
  push:

jobs:
  test:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        php: [8.5]
        os: [ubuntu-22.04, ubuntu-24.04, ubuntu-26.04]
    steps:
      - run: |
         sudo apt-get install libbrotli-dev libc-ares-dev libevent-dev libgpgme-dev libmaxminddb-dev libmcrypt-dev libmpdec-dev libnghttp2-dev libssh2-1-dev libssl-dev uuid-dev
