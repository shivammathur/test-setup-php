name: runner

on:
  push:

jobs:
  build:
    strategy:
      matrix:
        os: [macos-13, macos-14]
    runs-on: ${{ matrix.os }}
    steps:
      - name: Test
        run: brew --prefix
      - uses: mxschmitt/action-tmate@v3.17
