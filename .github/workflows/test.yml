name: tests

on: [push]

jobs:
  tests:
    runs-on: ubuntu-20.04

    strategy:
      fail-fast: true

    name: Runner

    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          extensions: scoutapm
