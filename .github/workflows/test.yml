name: Runner
on:
  push:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:      
      - name: Setup PHP
        uses: shivammathur/setup-php@verbose
        with:
          php-version: 7.4
          extensions: rdkafka
      - run: |
          php -v
          php -m
        
