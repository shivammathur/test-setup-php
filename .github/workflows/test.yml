name: Runner

on:
  push:

jobs:
  build:
    runs-on: macos-15
    steps:
      - name: Test
        run: |
          cat /Library/Developer/CommandLineTools/SDKs/MacOS*.sdk/usr/include/libxml/xmlversion.h
