name: runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Test
        run: |
          dpkg --add-architecture i386
          sudo apt-get install php7.2:i386
          php -r "echo PHP_INT_SIZE";
