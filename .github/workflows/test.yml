name: HTTP workflow
on:
  push:
jobs:
  php-stan:
    runs-on: macos-latest
    steps:
      - uses: shivammathur/setup-php@more-verbose
        with:
          php-version: 8.1
          extensions: amqp
      - run: php -v && php -m
