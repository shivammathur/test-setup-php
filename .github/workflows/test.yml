name: Runner test
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-22.04, ubuntu-20.04]
    steps:
      - name: Test
        uses: shivammathur/setup-php@verbose
        with:
          extensions: gnupg
