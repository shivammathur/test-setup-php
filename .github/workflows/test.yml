name: runner

on:
  push:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 8.1
        env:
          phpts: zts
          debug: true
      - name: Test
        run: |
          php -v
          php -m
          phpdbg -V
