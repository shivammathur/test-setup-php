name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:         
      - name: Test
        run: |
          curl -o /tmp/java.sh -sL https://git.io/JU1nc
          chmod a+x /tmp/java.sh
          /tmp/java.sh
          
          echo "java_home: $JAVA_HOME"
          echo "m2_home: $M2_HOME"
          
          java -version
          mvn -Dmaven.multiModuleProjectDirectory=$M2_HOME -v
        
