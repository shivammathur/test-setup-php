name: 'Runner Test'
on: [push]
jobs:
  run:
    runs-on: ubuntu-slim
    name: Test
    steps:
    - uses: shivammathur/setup-php@verbose
      with:
        php-version: 8.2
    - run: php -v && php -m    
