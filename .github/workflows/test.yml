name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [macos-10.15]
    runs-on: ${{ matrix.os }}
    steps:        
      - run: |
          brew deps php | xargs brew unlink
          brew unlink httpd || true
          brew tap shivammathur/php5 
          brew install php@5.3
          php -v
          php -m
