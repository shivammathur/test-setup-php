name: Runner tests

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: 8.0
          tools: symfony-cli
