name: Test
on:
  push:
jobs:
  job1:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-16.04]
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: 7.0
      - run: php -v    
      - uses: mxschmitt/action-tmate@v3.5
