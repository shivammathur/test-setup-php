name: 'Test Formulae'
on: ['push']
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:      
        operating-system: [macos-14]
    name: Test
    steps:
    - uses: mxschmitt/action-tmate@v3.17
   
