name: Runner Workflow
on:
  push:
jobs:
  runner:
    runs-on: ubuntu-latest-arm
        
    steps:
        - uses: shivammathur/setup-php@v2
          with:
            php-version: 8.5
          env:
            use_package_cache: false
        - run: php -v && php -m    


