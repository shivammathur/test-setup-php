name: 'Test'
on:
  push:
jobs:
  run:
    strategy:
      matrix:
        os: [ubuntu-16.04, ubuntu-18.04, ubuntu-20.04]
    runs-on: ${{ matrix.os }}
    steps:
      - run: |
          sudo apt-get install libacl1-dev -y
