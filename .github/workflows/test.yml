name: Test
on:
  push:
jobs:
  runner:
    name: Test
    runs-on: macos-11
    strategy:
      matrix:
        php: [ '8.1', '8.2', '8.3', '8.4' ]
    steps:
      - uses: shivammathur/setup-php@verbose
        with:
          php-version: ${{ matrix.php }}
          extensions: redis
