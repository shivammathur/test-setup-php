name: Runner
on:
  push:

jobs:
  test:
    runs-on: ubuntu-22.04
    steps:
      - run: curl -s "https://downloads.php.net/~windows/php-sdk/deps/series/packages-8.5-vs17-x64-staging.txt" | grep apache
        
