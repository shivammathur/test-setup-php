name: Testos
on:
  push:
jobs:
  ubuntu1:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: 7.2
      - run: sudo apt-get install php7.4 -y
  ubuntu2:
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install php7.4 -y

