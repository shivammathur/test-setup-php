name: Test workflow
on: [push]
jobs:
  test:    
    name: test
    runs-on: ${{ matrix.os }}
    container: "ubuntu:bionic"
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Test exit 
        run: |
          cross="✗"
          version='5.3'
          echo "::error::\033[31;1m$cross \033[0m\033[34;1mPHP \033[0m\033[90;1mPHP $version is not supported on self-hosted runner\033[0m\n"
          exit 1
        
          
