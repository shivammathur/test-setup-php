name: Test runner workflow
on: [push]
jobs:
  systemd:
    runs-on: ${{ matrix.operating-systems }}
    strategy:
      fail-fast: false
      matrix:        
        operating-systems: [ubuntu-18.04, ubuntu-20.04]
    steps:
      - run: sudo systemctl start mysql && sudo systemctl status mysql

