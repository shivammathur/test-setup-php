name: Runner workflow
on:
  push:
  workflow_dispatch:
jobs:
  sync:
    name: sync
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]    
    runs-on: ${{ matrix.os }}
    steps:
      - run: |
          echo "echo \$GITHUB_WORKFLOW" > test.sh
          bash test.sh
