name: Runner
on:
  push:
jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      matrix:
        operating-system: [windows-latest]
    steps:

    - name: Setup PHP
      uses: shivammathur/setup-php@v2
      with:
        php-version: 8.5
