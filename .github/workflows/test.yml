name: Test
on:
  push:
jobs:
  run:
    runs-on: ubuntu-16.04
    steps:
      - name: Test
        run: |
          sudo apt-cache policy php-pear php[0-9].[0-9]-dev
                 
               
