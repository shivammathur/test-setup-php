name: Test extensions
on: [push]    
jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        php-versions: ['5.3', '5.4', '5.5', '5.6', '7.0', '7.1', '7.2', '7.3', '7.4', '8.0']
        operating-system: [ubuntu-latest, windows-latest, macos-latest]
    name: Test PHP ${{ matrix.php-versions }} on ${{ matrix.operating-system }}
    steps:
    - name: Checkout
      uses: actions/checkout@v2

    - name: Setup PHP
      uses: shivammathur/setup-php@verbose
      with:        
        php-version: ${{ matrix.php-versions }}
        extensions: mailparse, mbstring
      
    - name: Check PHP version
      run: php -v

    - name: Check PHP extensions
      run: php -m

    - name: Check mailparse
      run: php -r "if(! extension_loaded('mailparse')) {throw new Exception('mailparse not found');}"
      continue-on-error: true
