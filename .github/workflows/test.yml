name: Runner
on: [push]
jobs:
  runner:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [windows-latest]
    steps:
      - uses: shivammathur/setup-php@develop
        with:
          php-version: 8.2
          coverage: xdebug3
      - run: |  
          php -v php -m
          

