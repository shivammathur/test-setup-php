name: Test runner workflow
on: [push]
jobs:
  run:
    runs-on: ${{ matrix.operating-systems }}
    strategy:
      fail-fast: false
      matrix:        
        operating-systems: [macos-12-beta, macos-12.0-beta]
    steps:
      - run: echo "Hello, world!"

