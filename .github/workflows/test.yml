name: PHP Tests
on: push
jobs:
    job:
        runs-on: ${{ matrix.os }}
        strategy:
            matrix:
                os: ['ubuntu-20.04']
        steps:
          - run: composer config -g allow-plugins true
          - run: composer -vvv global require symfony/flex:1.10.0
