name: 'Runner Test'
on: [push]
jobs:
  run:
    runs-on: windows-latest
    name: Test
    steps:
    - uses: actions/checkout@v6
      with:
        repository: xdebug/xdebug
    - run: |
        Write-Host $Env:BUILD_DIRECTORY
