name: Testos
on:
  push:
jobs:
  ubuntu1:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: 7.2
      - uses: mxschmitt/action-tmate@v3.6

