name: Runner
on:
  push:
jobs:
  ubuntu:
    name: ubuntu
    runs-on: test-dcr
    strategy:
      fail-fast: false
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@more-verbose
        with:
          extensions: gnupg
      - name: Testing PHP version
        run: |
          php -v
