name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest]
    name: Test
    runs-on: ${{ matrix.operating-system }}
    steps:
    - uses: shivammathur/setup-php@develop
      with:
        php-version: 8.3
        ini-file: development
        coverage: none
        tools: none
      env:
        phpts: ts
        debug: true
    - run: |
        ls /tmp
        php -v
        objdump --syms $(which php)
        
          
