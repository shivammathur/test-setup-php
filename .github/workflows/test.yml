name: Runner
on:
  push:
jobs:
    runner:
        runs-on: ubuntu-latest
        steps:
        - name: Setup PHP
          uses: shivammathur/setup-php@verbose
          with:
            php-version: '8.1'
        - uses: mxschmitt/action-tmate@v3.17
