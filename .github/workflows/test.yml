name: Test
on:
  push:
jobs:
  runner:
    strategy:
      matrix:
        runs: [1,1,1,1,1,1,1,1]
    name: Test
    runs-on: ubuntu-latest
    steps:
      - run: sudo apt-get install libgd-dev || exit 1
