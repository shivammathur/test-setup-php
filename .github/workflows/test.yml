on:
  - push

name: build

jobs:
  tests:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os:
          - windows-2016
          - windows-latest
    steps:
    - run: |
        ls "C:\Program Files\7-Zip"
        7z -mqs 2>&1 | Out-Null
          
