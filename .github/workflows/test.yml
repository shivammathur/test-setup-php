name: Testos
on:
  push:
jobs:
  ubuntu:
    name: testos
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: 7.2
      - run: apt-get install php7.4 -y
