name: Test workflow
on: [push]
jobs:
  test:    
    name: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Test exit 
        continue-on-error: true
        run: |
          printf "run() { \nif true; then printf \"X PHP PHP 5.3 is not supported on self-hosted runner\"; fi && exit 1 \n} \nrun;" > linux.sh
          cat linux.sh
          bash linux.sh

      - name: Should not run
        run: echo 'test'
        
          
