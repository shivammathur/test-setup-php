name: Runner
on:
  push:
jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php: [8.5]
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@more-verbose
        with:
            php-version: ${{ matrix.php }}
      - run: php -v && php -m    
