name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2019]
    steps:    
      - name: Test
        run: |
          Remove-Item "C:\Program Files\Git\usr\bin\printf.exe" -Force
          printf "Test"
        
