name: 'Test'
on: [push]
jobs:
  tests:  
    strategy:
      matrix:
        os: [macos-11.0]        
    runs-on: ${{ matrix.os }}
    steps:        
      - run: |
          brew tap shivammathur/php5 
          brew install php@5.3
          php -v
          php -m
