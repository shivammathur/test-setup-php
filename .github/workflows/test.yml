name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-10.13, macos-10.14, macos-10.15, macos-11.0]
    steps:    
      - name: Test
        run: sw_vers
        
