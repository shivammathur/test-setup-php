name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        operating-system: [macos-13]
    name: Test
    runs-on: ${{ matrix.operating-system }}
    steps:
      - uses: mxschmitt/action-tmate@v3.17
          
