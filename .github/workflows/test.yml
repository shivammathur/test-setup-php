name: Runner
on:
  push:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        uses: shivammathur/setup-php@more-verbose
        with:
          extensions: redis-5.3.7, rdkafka-4.1.2
      - run: |
          php -r "echo phpversion('redis') . PHP_EOL;"
          php -r "echo phpversion('rdkafka') . PHP_EOL;"
          php -v
          php -m
