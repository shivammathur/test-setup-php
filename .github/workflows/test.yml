name: Runner workflow
on:
  push:
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
        ext: ["", "-2.5.2"]
        php-version:
          - 8.1
          - 8.2
          - 8.3
          - 8.4
          - 8.5
          - 8.6
          
    steps:
      - name: Install PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: ${{ matrix.php-version }}
          extensions: mongodb${{ matrix.ext }}
      - run: |
          php -r "echo phpversion('mongodb');"
          php -v
          php -m
      
  
