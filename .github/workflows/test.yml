name: Runner tests

on: push

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - name: Install PHP
        uses: shivammathur/setup-php@v2
        with:
            php-version: '8.0'
            tools: symfony-cli
