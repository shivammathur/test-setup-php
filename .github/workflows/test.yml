name: tests

on: [push]

jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest, windows-latest, macos-latest]
    steps:
      - name: Install PHP
        uses: pyrech/setup-php@support-castor
        with:
          php-version: 8.1
          tools: castor
      - run: php -v && castor -V
