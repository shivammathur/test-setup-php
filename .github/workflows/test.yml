  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: Test
    runs-on: macos-latest
    strategy:
      fail-fast: false
    steps:
      - run: curl -SsL https://github.com/xdebug/xdebug/tags | awk '/\/tag\/([0-9]+.[0-9]+.[0-9]+)">/' | cut -d '"' -f 2 | awk '{n=split($NF,a,"/");print a[n]}' | head -n 1
