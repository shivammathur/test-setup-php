name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-2025]
    runs-on: ${{matrix.os}}
    steps:
      - uses: mxschmitt/action-tmate@v3.22
      
