  
name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
         os: [windows-latest]
    steps:
      - name: Test
        run: |
          Measure-Command { powershell -c "echo test" }
          Measure-Command { pwsh -c "echo test" }
