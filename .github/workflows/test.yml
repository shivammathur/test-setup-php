
name: 'Test Formulae'
on:
  push:
jobs:
  tests:
    if: "!contains(github.event.head_commit.message, 'skip-test')"
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
    steps:
      - run: echo 'Hello, world'
