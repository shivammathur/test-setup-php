name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [macos-latest]
    steps:
      - uses: shivammathur/setup-php@releases/v1
        with:
          php-version: '5.5'
          wrong_key: 'test'
