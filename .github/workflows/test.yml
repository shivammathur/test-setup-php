name: Runner
on: [ push]

jobs:
  php:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, macos-latest]
    steps:
      - uses: shivammathur/setup-php@v2  
        with:
          php-version: 8.4
          extensions: redis
      - run: php --ri redis    

