
name: Runner Workflow
on:
  push:
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-22.04]
    steps:
      - uses: shivammathur/setup-php@v2
        with:
          php-version: '8.1'
          tools: phpunit
      - run: |
          phpunit version

