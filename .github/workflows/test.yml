name: Runner workflow
on:
  push:
env:
  php-version: 7.3
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: 7.4
      - name: Setup PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: 7.4
