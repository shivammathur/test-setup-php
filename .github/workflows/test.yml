name: Test workflow
on: [push]
jobs:
  test:    
    name: test
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Test exit 
        run: |
          echo 'printf "X PHP PHP 5.3 is not supported on self-hosted runner" && exit 1' > linux.sh
          cat linux.sh
          bash linux.sh

      - name: Should not run
        run: echo 'test'
        
          
