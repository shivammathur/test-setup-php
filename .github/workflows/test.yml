name: Test
on:
  push:
jobs:
  runner:
    strategy:
      fail-fast: false
      matrix:
        os: [macos-14-intel, macos-15-intel]
    runs-on: ${{matrix.os}}
    steps:
      - uses: shivammathur/setup-php@v2
