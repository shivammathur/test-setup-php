name: 'Windows workflow' #test
on:
  push:
jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [windows-latest]
    name: Test on ${{ matrix.operating-system }}
    steps:
      - name: install php
        uses: shivammathur/setup-php@develop
        with: 
          php-version: 7.4
      - name: test
        run: |
          php -v
          Get-ChildItem C:\tools\php
          
              
         
