name: Runner workflow
on:
  push:
jobs:
  ubuntu:
    name: runner    
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-2019, windows-2016]
    steps:    
      - name: Test
        run: |
          pwsh -c "Write-Host 'Red text.' -ForegroundColor red" || true
        
