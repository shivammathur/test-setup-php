name: Source extensions workflow
on:
  push:
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: shivammathur/setup-php@more-verbose
        with:
          php-version: 8.1
          tools: vapor:1.11
      - run: vapor -V    
