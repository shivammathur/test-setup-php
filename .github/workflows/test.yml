name: 'Test'
on:
  push:
jobs:
  run:
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest]
    steps:
      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          extensions: protobuf-protocolbuffers/protobuf@master, opencc-nauxliu/opencc4php@master
        env:
          OPENCC_LIBS: libopencc-dev
          PROTOBUF_PATH: php/ext/google/protobuf
      - run: php -v && php -m   
          
