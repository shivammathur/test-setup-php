
name: Runner Workflow
on:
  push:
jobs:
  runner:
    runs-on: ${{ matrix.os }}
    container: ubuntu:24.04
    strategy:
      matrix:
        os: [ubuntu-22.04-arm, ubuntu-24.04-arm]
    steps:
      - run: php -v && php -m
