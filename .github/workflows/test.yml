name: Runner workflow
on: [push]
jobs:
  run:
    name: 'Test'
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [macos-12, macos-13, macos-14, macos-15]
    steps:        
        - run: php -v || true
        
        
