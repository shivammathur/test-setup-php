name: 'Cubrid workflow'
on:
  push:
env:
  key: cache-v3-2.37.0
  extensions: cubrid, pdo_cubrid
jobs:
  run:
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:
        operating-system: [ubuntu-latest]
        php-versions: ['5.3', '5.4', '5.5', '5.6', '7.0', '7.1', '7.2', '7.3', '7.4']
    name: PHP ${{ matrix.php-versions }} Test on ${{ matrix.operating-system }}
    steps:
      - name: Resolve extensions
        id: resolve
        shell: bash
        run: |
          extensions='${{ env.extensions }}'
          if [[ "${{ matrix.php-versions }}" == "7.3" || "${{ matrix.php-versions }}" == "7.4" ]]; then
            extensions='cubrid'
          fi
          echo "extensions=$extensions" >> "$GITHUB_OUTPUT"

      - name: Setup cache environment
        id: extcache
        uses: shivammathur/cache-extensions@1.14.1
        with:
          php-version: ${{ matrix.php-versions }}
          extensions: ${{ steps.resolve.outputs.extensions }}
          key: ${{ env.key }}

      - name: Cache extensions
        uses: actions/cache@v5.0.3
        with:
          path: ${{ steps.extcache.outputs.dir }}
          key: ${{ steps.extcache.outputs.key }}
          restore-keys: ${{ steps.extcache.outputs.key }}

      - name: Install PHP
        uses: shivammathur/setup-php@develop
        with:
          php-version: ${{ matrix.php-versions }}
          extensions: ${{ steps.resolve.outputs.extensions }}

      - name: Testing PHP version
        run: php -v

      - name: Testing Extensions
        run: |
          php -m
          php -r "if(! extension_loaded('cubrid')) {throw new Exception('cubrid not found');}"
          php -r "if(phpversion()<=7.2 && ! extension_loaded('pdo_cubrid')) {throw new Exception('pdo_cubrid not found');}"
