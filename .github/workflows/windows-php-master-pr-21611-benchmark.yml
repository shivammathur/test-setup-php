name: Benchmark PHP master PR 21611 on Windows

on:
  workflow_dispatch:
    inputs:
      baseline-artifact-run-id:
        description: Reuse baseline build artifact from a previous workflow run ID
        required: true
      clang-artifact-run-id:
        description: Reuse clang build artifact from a previous workflow run ID
        required: true

permissions:
  contents: read

jobs:
  build-msvc-baseline:
    name: Build PHP master baseline (MSVC+PGO)
    if: ${{ false }}
    runs-on: windows-2022
    timeout-minutes: 150
    env:
      BUILDER_REPOSITORY: ${{ github.event_name == 'workflow_dispatch' && inputs['builder-repository'] || 'shivammathur/php-windows-builder' }}
      BUILDER_REF: ${{ github.event_name == 'workflow_dispatch' && inputs['builder-ref'] || 'codex/pgo-case-revival' }}
      PHP_SDK_REPOSITORY: ${{ github.event_name == 'workflow_dispatch' && inputs['php-sdk-repository'] || 'shivammathur/php-sdk-binary-tools' }}
      PHP_SDK_REF: ${{ github.event_name == 'workflow_dispatch' && inputs['php-sdk-ref'] || 'codex/pgo-case-revival' }}
      PGO_CASES: ${{ github.event_name == 'workflow_dispatch' && inputs['pgo-cases'] || 'pgo01org,symfony_demo' }}
    steps:
      - name: Checkout test app
        uses: actions/checkout@v6

      - name: Checkout php-windows-builder
        uses: actions/checkout@v6
        with:
          repository: ${{ env.BUILDER_REPOSITORY }}
          ref: ${{ env.BUILDER_REF }}
          path: php-windows-builder

      - name: Build PHP master with php-windows-builder
        uses: ./php-windows-builder/php
        with:
          php-version: master
          arch: x64
          ts: ts
          php-sdk-repository: ${{ env.PHP_SDK_REPOSITORY }}
          php-sdk-ref: ${{ env.PHP_SDK_REF }}
          pgo-cases: ${{ env.PGO_CASES }}
          github-token: ${{ github.token }}

      - name: Prepare baseline artifact
        shell: pwsh
        run: |
          $zip = Get-ChildItem artifacts -Filter 'php-*.zip' |
            Where-Object { $_.Name -notmatch 'src|devel|debug|test' } |
            Sort-Object Name |
            Select-Object -First 1

          if ($null -eq $zip) {
            throw 'Could not find a baseline runtime zip in artifacts/'
          }

          $extractDir = Join-Path $env:RUNNER_TEMP 'baseline-extract'
          $dest = Join-Path $env:RUNNER_TEMP 'php-master-msvc-pgo'

          if (Test-Path $extractDir) {
            Remove-Item $extractDir -Recurse -Force
          }

          if (Test-Path $dest) {
            Remove-Item $dest -Recurse -Force
          }

          Expand-Archive -LiteralPath $zip.FullName -DestinationPath $extractDir -Force

          $phpExe = Get-ChildItem $extractDir -Filter php.exe -Recurse | Select-Object -First 1
          if ($null -eq $phpExe) {
            throw "Could not find php.exe after extracting $($zip.Name)"
          }

          New-Item -ItemType Directory -Path $dest -Force | Out-Null
          Copy-Item -Path (Join-Path $phpExe.Directory.FullName '*') -Destination $dest -Recurse -Force

      - name: Smoke test baseline build
        shell: pwsh
        run: |
          $php = Join-Path $env:RUNNER_TEMP 'php-master-msvc-pgo\php.exe'
          & $php -n -r "echo PHP_VERSION, ' ', PHP_ZTS ? 'TS' : 'NTS', PHP_EOL;"
          if ($LASTEXITCODE -ne 0) {
            throw 'Baseline PHP smoke test failed'
          }

      - name: Upload baseline build
        uses: actions/upload-artifact@v4
        with:
          name: php-master-msvc-pgo-build
          path: ${{ runner.temp }}/php-master-msvc-pgo
          if-no-files-found: error

  build-clang-pr:
    name: Build PHP master + PR 21611 (clang)
    if: ${{ false }}
    runs-on: windows-2022
    timeout-minutes: 150
    env:
      PHP_PR: 21611
      PHP_BUILD_CACHE_BASE_DIR: C:\build-cache
      PHP_BUILD_OBJ_DIR: C:\obj
      PHP_BUILD_CACHE_SDK_DIR: C:\build-cache\sdk
      PHP_BUILD_SDK_BRANCH: php-sdk-2.5.0
      PHP_BUILD_CRT: vs17
      PLATFORM: x64
      THREAD_SAFE: "1"
      INTRINSICS: AVX2
      CLANG_TOOLSET: "1"
    steps:
      - name: Checkout test app
        uses: actions/checkout@v6

      - name: Clone php-src master
        shell: pwsh
        run: |
          git config --global core.autocrlf false
          git config --global core.eol lf
          git clone --depth 1 --branch master https://github.com/php/php-src.git php-src-pr

      - name: Apply PR 21611 patch
        shell: pwsh
        working-directory: php-src-pr
        run: |
          $patch = Join-Path $env:RUNNER_TEMP "php-pr-$env:PHP_PR.diff"
          Invoke-WebRequest "https://patch-diff.githubusercontent.com/raw/php/php-src/pull/$env:PHP_PR.diff" -OutFile $patch
          git apply --check --3way $patch
          git apply --3way $patch

      - name: Build patched PHP with clang
        shell: cmd
        working-directory: php-src-pr
        run: .github\scripts\windows\build.bat

      - name: Prepare clang artifact
        shell: pwsh
        run: |
          $sourceDir = 'C:\obj\Release_TS'
          $phpExe = Join-Path $sourceDir 'php.exe'
          $dest = Join-Path $env:RUNNER_TEMP 'php-master-pr-21611-clang'

          if (!(Test-Path $phpExe)) {
            throw "Missing clang build php.exe at $phpExe"
          }

          if (Test-Path $dest) {
            Remove-Item $dest -Recurse -Force
          }

          New-Item -ItemType Directory -Path $dest -Force | Out-Null
          Copy-Item -Path (Join-Path $sourceDir '*') -Destination $dest -Recurse -Force

      - name: Smoke test clang build
        shell: pwsh
        run: |
          $php = Join-Path $env:RUNNER_TEMP 'php-master-pr-21611-clang\php.exe'
          $meta = & $php -n -r "echo PHP_VERSION, ' ', PHP_ZTS ? 'TS' : 'NTS', ' ', ZEND_VM_KIND;"

          if ($LASTEXITCODE -ne 0) {
            throw 'Clang PHP smoke test failed'
          }

          if ($meta -notmatch 'ZEND_VM_KIND_TAILCALL') {
            throw "Expected ZEND_VM_KIND_TAILCALL, got: $meta"
          }

          Write-Host $meta

      - name: Upload clang build
        uses: actions/upload-artifact@v4
        with:
          name: php-master-pr-21611-clang-build
          path: ${{ runner.temp }}/php-master-pr-21611-clang
          if-no-files-found: error

  benchmark:
    name: Benchmark master baseline vs PR 21611 clang build
    if: ${{ inputs['baseline-artifact-run-id'] != '' && inputs['clang-artifact-run-id'] != '' }}
    runs-on: windows-2022
    timeout-minutes: 60
    env:
      BASELINE_ARTIFACT_RUN_ID: ${{ inputs['baseline-artifact-run-id'] }}
      CLANG_ARTIFACT_RUN_ID: ${{ inputs['clang-artifact-run-id'] }}
      BENCHMARK_ITERATIONS: 5
      PHPBENCH_ITERATIONS: 5
      K6_DURATION: 15s
      K6_VUS: 10
    steps:
      - name: Checkout test app
        uses: actions/checkout@v6

      - name: Setup PHP for Composer
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.4"
          extensions: intl, mbstring
          tools: composer
          coverage: none

      - name: Download baseline build from previous run
        uses: actions/download-artifact@v4
        with:
          name: php-master-msvc-pgo-build
          path: C:\php-master-msvc-pgo
          repository: ${{ github.repository }}
          run-id: ${{ env.BASELINE_ARTIFACT_RUN_ID }}
          github-token: ${{ github.token }}

      - name: Download clang build from previous run
        uses: actions/download-artifact@v4
        with:
          name: php-master-pr-21611-clang-build
          path: C:\php-master-pr-21611-clang
          repository: ${{ github.repository }}
          run-id: ${{ env.CLANG_ARTIFACT_RUN_ID }}
          github-token: ${{ github.token }}

      - name: Install application dependencies
        shell: pwsh
        run: |
          composer install --no-progress --prefer-dist --optimize-autoloader
          composer require --dev phpbench/phpbench:^1.6 --no-interaction --no-progress
          composer run-script post-install-cmd --no-interaction

      - name: Install k6
        shell: pwsh
        run: |
          if (Get-Command k6 -ErrorAction SilentlyContinue) {
            k6 version
            exit 0
          }

          choco install k6 -y --no-progress
          k6 version

      - name: Prepare benchmark configuration
        shell: pwsh
        run: |
          $baselinePhp = Get-ChildItem 'C:\php-master-msvc-pgo' -Filter php.exe -Recurse | Select-Object -First 1 -ExpandProperty FullName
          $customPhp = Get-ChildItem 'C:\php-master-pr-21611-clang' -Filter php.exe -Recurse | Select-Object -First 1 -ExpandProperty FullName

          foreach ($php in @($baselinePhp, $customPhp)) {
            if ([string]::IsNullOrWhiteSpace($php) -or !(Test-Path $php)) {
              throw "Missing php.exe at $php"
            }
          }

          $baselineDir = Split-Path -Parent $baselinePhp
          $customDir = Split-Path -Parent $customPhp
          $baselinePhpBenchBin = 'phpbench-baseline.exe'
          $customPhpBenchBin = 'phpbench-custom.exe'
          $baselinePhpBenchPath = Join-Path $baselineDir $baselinePhpBenchBin
          $customPhpBenchPath = Join-Path $customDir $customPhpBenchBin

          function Get-BenchmarkConfig {
            param(
              [string]$PhpDir,
              [string]$JitMode
            )

            function Find-ExtensionPath {
              param(
                [string]$SearchDir,
                [string]$FileName,
                [switch]$Optional
              )

              $candidates = @(
                (Join-Path $SearchDir "ext\$FileName"),
                (Join-Path $SearchDir $FileName)
              )

              foreach ($candidate in $candidates) {
                if (Test-Path $candidate) {
                  return $candidate
                }
              }

              $found = Get-ChildItem $SearchDir -Filter $FileName -Recurse | Select-Object -First 1 -ExpandProperty FullName
              if (![string]::IsNullOrWhiteSpace($found)) {
                return $found
              }

              if ($Optional) {
                return $null
              }

              throw "Missing required extension $FileName under $SearchDir"
            }

            $mbstring = Find-ExtensionPath -SearchDir $PhpDir -FileName 'php_mbstring.dll'
            $intl = Find-ExtensionPath -SearchDir $PhpDir -FileName 'php_intl.dll'
            $openssl = Find-ExtensionPath -SearchDir $PhpDir -FileName 'php_openssl.dll'
            $opcache = Find-ExtensionPath -SearchDir $PhpDir -FileName 'php_opcache.dll' -Optional

            $mbstringPosix = $mbstring -replace '\\', '/'
            $intlPosix = $intl -replace '\\', '/'
            $opensslPosix = $openssl -replace '\\', '/'

            $config = [ordered]@{
              'date.timezone' = 'UTC'
              'display_errors' = 1
              'error_reporting' = 32767
              'memory_limit' = '1G'
              'extension' = @($opensslPosix, $mbstringPosix, $intlPosix)
              'opcache.enable' = 1
              'opcache.enable_cli' = 1
              'opcache.jit_buffer_size' = '128M'
              'opcache.jit' = $JitMode
            }

            if ($null -ne $opcache) {
              $config['zend_extension'] = ($opcache -replace '\\', '/')
            }

            return $config
          }

          function Write-BenchmarkIni {
            param(
              [hashtable]$Config,
              [string]$IniPath
            )

            $lines = @()

            foreach ($entry in $Config.GetEnumerator()) {
              $values = @($entry.Value)

              foreach ($value in $values) {
                if ($entry.Key -eq 'zend_extension') {
                  $lines += ('zend_extension="{0}"' -f $value)
                } elseif ($entry.Key -eq 'extension') {
                  $lines += ('extension="{0}"' -f $value)
                } else {
                  $lines += ('{0}={1}' -f $entry.Key, $value)
                }
              }
            }

            Set-Content -Path $IniPath -Value $lines
          }

          $baselineOffConfig = Get-BenchmarkConfig -PhpDir $baselineDir -JitMode 'off'
          $baselineTracingConfig = Get-BenchmarkConfig -PhpDir $baselineDir -JitMode 'tracing'
          $customOffConfig = Get-BenchmarkConfig -PhpDir $customDir -JitMode 'off'
          $customTracingConfig = Get-BenchmarkConfig -PhpDir $customDir -JitMode 'tracing'

          $baselineIni = Join-Path $env:RUNNER_TEMP 'baseline-bench.ini'
          $customIni = Join-Path $env:RUNNER_TEMP 'custom-bench.ini'

          Write-BenchmarkIni -Config $baselineOffConfig -IniPath $baselineIni
          Write-BenchmarkIni -Config $customOffConfig -IniPath $customIni
          Copy-Item -Path $baselinePhp -Destination $baselinePhpBenchPath -Force
          Copy-Item -Path $customPhp -Destination $customPhpBenchPath -Force

          "BASELINE_PHP=$baselinePhp" >> $env:GITHUB_ENV
          "CUSTOM_PHP=$customPhp" >> $env:GITHUB_ENV
          "BASELINE_INI=$baselineIni" >> $env:GITHUB_ENV
          "CUSTOM_INI=$customIni" >> $env:GITHUB_ENV
          "BASELINE_PHPBENCH_BIN=$baselinePhpBenchBin" >> $env:GITHUB_ENV
          "CUSTOM_PHPBENCH_BIN=$customPhpBenchBin" >> $env:GITHUB_ENV
          "BASELINE_PHPBENCH_OFF_CONFIG=$($baselineOffConfig | ConvertTo-Json -Compress -Depth 4)" >> $env:GITHUB_ENV
          "BASELINE_PHPBENCH_TRACING_CONFIG=$($baselineTracingConfig | ConvertTo-Json -Compress -Depth 4)" >> $env:GITHUB_ENV
          "CUSTOM_PHPBENCH_OFF_CONFIG=$($customOffConfig | ConvertTo-Json -Compress -Depth 4)" >> $env:GITHUB_ENV
          "CUSTOM_PHPBENCH_TRACING_CONFIG=$($customTracingConfig | ConvertTo-Json -Compress -Depth 4)" >> $env:GITHUB_ENV
          $baselineDir >> $env:GITHUB_PATH
          $customDir >> $env:GITHUB_PATH

      - name: Smoke test both PHP executables
        shell: pwsh
        run: |
          $baselineMeta = & $env:BASELINE_PHP -n -r "echo PHP_VERSION, ' ', PHP_ZTS ? 'TS' : 'NTS';"
          if ($LASTEXITCODE -ne 0) {
            throw 'Baseline PHP smoke test failed'
          }

          $customMeta = & $env:CUSTOM_PHP -n -r "echo PHP_VERSION, ' ', PHP_ZTS ? 'TS' : 'NTS', ' ', ZEND_VM_KIND;"
          if ($LASTEXITCODE -ne 0) {
            throw 'Clang PHP smoke test failed'
          }

          $baselinePhpBenchCommand = Get-Command $env:BASELINE_PHPBENCH_BIN -ErrorAction Stop
          $customPhpBenchCommand = Get-Command $env:CUSTOM_PHPBENCH_BIN -ErrorAction Stop

          if ($customMeta -notmatch 'ZEND_VM_KIND_TAILCALL') {
            throw "Expected ZEND_VM_KIND_TAILCALL, got: $customMeta"
          }

          $baselineExtensions = & $env:BASELINE_PHP -c $env:BASELINE_INI -r "echo (int)extension_loaded('mbstring'), (int)extension_loaded('intl'), (int)extension_loaded('Zend OPcache');"
          if ($LASTEXITCODE -ne 0 -or $baselineExtensions -ne '111') {
            throw "Baseline benchmark ini did not load required extensions: $baselineExtensions"
          }

          $customExtensions = & $env:CUSTOM_PHP -c $env:CUSTOM_INI -r "echo (int)extension_loaded('mbstring'), (int)extension_loaded('intl'), (int)extension_loaded('Zend OPcache');"
          if ($LASTEXITCODE -ne 0 -or $customExtensions -ne '111') {
            throw "Clang benchmark ini did not load required extensions: $customExtensions"
          }

          Write-Host $baselineMeta
          Write-Host $customMeta
          Write-Host ('Resolved baseline PHPBench binary: {0}' -f $baselinePhpBenchCommand.Source)
          Write-Host ('Resolved custom PHPBench binary: {0}' -f $customPhpBenchCommand.Source)

      - name: Run benchmarks
        shell: pwsh
        run: |
          $iterations = [int]$env:BENCHMARK_ITERATIONS
          $phpBenchIterations = [int]$env:PHPBENCH_ITERATIONS
          $resultsDir = Join-Path $env:RUNNER_TEMP 'benchmark-results'
          New-Item -ItemType Directory -Path $resultsDir -Force | Out-Null

          $timeBenchmarks = @(
            @{
              Name = 'vm-jit-off'
              Args = @('-d', 'opcache.jit=off', '.github/benchmarks/vm.php', '50000')
            },
            @{
              Name = 'vm-jit-tracing'
              Args = @('-d', 'opcache.jit=tracing', '.github/benchmarks/vm.php', '50000')
            },
            @{
              Name = 'cake-bootstrap-jit-off'
              Args = @('-d', 'opcache.jit=off', '.github/benchmarks/cake_bootstrap.php', '150')
            },
            @{
              Name = 'cake-bootstrap-jit-tracing'
              Args = @('-d', 'opcache.jit=tracing', '.github/benchmarks/cake_bootstrap.php', '150')
            }
          )

          function Invoke-TimeBenchmarkSuite {
            param(
              [string]$Label,
              [string]$PhpExe,
              [string]$IniFile,
              [string]$OutputFile
            )

            $suite = @()

            foreach ($benchmark in $timeBenchmarks) {
              $times = @()

              for ($run = 1; $run -le $iterations; $run++) {
                $args = @('-c', $IniFile) + $benchmark.Args
                $watch = [System.Diagnostics.Stopwatch]::StartNew()
                $previousScanDir = $env:PHP_INI_SCAN_DIR

                try {
                  $env:PHP_INI_SCAN_DIR = ''
                  $output = & $PhpExe @args 2>&1
                  $exitCode = $LASTEXITCODE
                } finally {
                  if ($null -eq $previousScanDir) {
                    Remove-Item Env:PHP_INI_SCAN_DIR -ErrorAction SilentlyContinue
                  } else {
                    $env:PHP_INI_SCAN_DIR = $previousScanDir
                  }
                }

                $watch.Stop()

                if ($exitCode -ne 0) {
                  $outputText = ($output | Out-String).Trim()
                  if (![string]::IsNullOrWhiteSpace($outputText)) {
                    Write-Host $outputText
                  }
                  throw ('{0} failed benchmark ''{1}'' on run {2}' -f $Label, $benchmark.Name, $run)
                }

                $times += [Math]::Round($watch.Elapsed.TotalMilliseconds, 2)
              }

              $stats = $times | Measure-Object -Average -Minimum -Maximum
              $suite += [pscustomobject]@{
                php = $Label
                benchmark = $benchmark.Name
                runs_ms = $times
                avg_ms = [Math]::Round($stats.Average, 2)
                min_ms = [Math]::Round($stats.Minimum, 2)
                max_ms = [Math]::Round($stats.Maximum, 2)
              }
            }

            $suite | ConvertTo-Json -Depth 4 | Set-Content -Path $OutputFile
            return $suite
          }

          function Invoke-PhpBenchMetric {
            param(
              [string]$Label,
              [string]$PhpBinary,
              [string]$PhpConfig,
              [string]$BenchmarkName,
              [string]$OutputFile,
              [string]$XmlFile
            )

            $args = @(
              'vendor/bin/phpbench',
              'run',
              '.github/benchmarks/phpbench',
              '--progress=none',
              '--report=default',
              '--filter',
              'SyntheticCliBench::benchCompositeWorkload',
              '--dump-file',
              $XmlFile,
              '--iterations',
              $phpBenchIterations,
              '--revs',
              '1',
              '--warmup',
              '1',
              '--php-binary',
              $PhpBinary,
              '--php-config',
              $PhpConfig,
              '--php-disable-ini'
            )

            $output = & php @args 2>&1
            if ($LASTEXITCODE -ne 0) {
              $outputText = ($output | Out-String).Trim()
              if (![string]::IsNullOrWhiteSpace($outputText)) {
                Write-Host $outputText
              }
              throw ('{0} failed benchmark ''{1}''' -f $Label, $BenchmarkName)
            }

            [xml]$xml = Get-Content -Path $XmlFile -Raw
            $variant = $xml.phpbench.suite.benchmark.subject.variant
            if ($null -eq $variant -or $null -eq $variant.stats) {
              throw ('Missing PHPBench stats for {0}' -f $BenchmarkName)
            }

            $meanUs = [double]$variant.stats.mean
            $opsPerSecond = [Math]::Round(1000000 / $meanUs, 2)

            $result = [pscustomobject]@{
              php = $Label
              benchmark = $BenchmarkName
              unit = 'ops/s'
              value = $opsPerSecond
              mean_us = [Math]::Round($meanUs, 2)
              revs = [int]$variant.revs
              iterations = $phpBenchIterations
            }

            $result | ConvertTo-Json -Depth 4 | Set-Content -Path $OutputFile
            return $result
          }

          function Wait-ForHttp {
            param(
              [string]$Url,
              [int]$Attempts = 30
            )

            for ($attempt = 1; $attempt -le $Attempts; $attempt++) {
              try {
                $response = Invoke-WebRequest -Uri $Url -UseBasicParsing -TimeoutSec 5
                if ($response.StatusCode -eq 200) {
                  return
                }
              } catch {
              }

              Start-Sleep -Seconds 1
            }

            throw "Timed out waiting for $Url"
          }

          function Start-PhpHttpServer {
            param(
              [string]$PhpExe,
              [string]$IniFile,
              [string]$JitMode,
              [int]$Port,
              [string]$Label
            )

            $stdout = Join-Path $resultsDir ('{0}-{1}-stdout.log' -f $Label, $Port)
            $stderr = Join-Path $resultsDir ('{0}-{1}-stderr.log' -f $Label, $Port)
            $docroot = Join-Path $PWD.Path 'webroot'
            $router = Join-Path $PWD.Path 'webroot\index.php'
            $url = 'http://127.0.0.1:{0}/' -f $Port
            $args = @(
              '-c',
              $IniFile,
              '-d',
              ('opcache.jit={0}' -f $JitMode),
              '-S',
              ('127.0.0.1:{0}' -f $Port),
              '-t',
              $docroot,
              $router
            )

            $previousScanDir = $env:PHP_INI_SCAN_DIR

            try {
              $env:PHP_INI_SCAN_DIR = ''
              $process = Start-Process -FilePath $PhpExe -ArgumentList $args -WorkingDirectory $PWD.Path -RedirectStandardOutput $stdout -RedirectStandardError $stderr -PassThru
            } finally {
              if ($null -eq $previousScanDir) {
                Remove-Item Env:PHP_INI_SCAN_DIR -ErrorAction SilentlyContinue
              } else {
                $env:PHP_INI_SCAN_DIR = $previousScanDir
              }
            }

            try {
              Wait-ForHttp -Url $url
            } catch {
              if (!$process.HasExited) {
                Stop-Process -Id $process.Id -Force
              }

              if (Test-Path $stdout) {
                Get-Content $stdout | Write-Host
              }

              if (Test-Path $stderr) {
                Get-Content $stderr | Write-Host
              }

              throw
            }

            return [pscustomobject]@{
              Process = $process
              Url = $url
              StdOut = $stdout
              StdErr = $stderr
            }
          }

          function Stop-PhpHttpServer {
            param(
              [pscustomobject]$Server
            )

            if ($null -ne $Server -and $null -ne $Server.Process -and !$Server.Process.HasExited) {
              Stop-Process -Id $Server.Process.Id -Force
            }
          }

          function Invoke-HttpRpsMetric {
            param(
              [string]$Label,
              [string]$PhpExe,
              [string]$IniFile,
              [int]$Port,
              [string]$OutputFile
            )

            $server = $null

            try {
              $server = Start-PhpHttpServer -PhpExe $PhpExe -IniFile $IniFile -JitMode 'off' -Port $Port -Label $Label
              $summaryFile = Join-Path $resultsDir ('{0}-http-rps-k6.json' -f $Label)

              $env:K6_TARGET = $server.Url
              $env:K6_NEEDLE = 'CakePHP'

              $output = & k6 run --vus $env:K6_VUS --duration $env:K6_DURATION --summary-export $summaryFile .github/benchmarks/http_rps.js 2>&1
              if ($LASTEXITCODE -ne 0) {
                $outputText = ($output | Out-String).Trim()
                if (![string]::IsNullOrWhiteSpace($outputText)) {
                  Write-Host $outputText
                }
                throw ('{0} failed benchmark ''http-rps''' -f $Label)
              }

              $summary = Get-Content -Path $summaryFile -Raw | ConvertFrom-Json
              $rate = [double]$summary.metrics.http_reqs.values.rate
              $p95 = [double]$summary.metrics.http_req_duration.values.'p(95)'

              $result = [pscustomobject]@{
                php = $Label
                benchmark = 'http-rps'
                unit = 'req/s'
                value = [Math]::Round($rate, 2)
                p95_ms = [Math]::Round($p95, 2)
                vus = [int]$env:K6_VUS
                duration = $env:K6_DURATION
              }

              $result | ConvertTo-Json -Depth 4 | Set-Content -Path $OutputFile
              return $result
            } finally {
              Stop-PhpHttpServer -Server $server
            }
          }

          function Format-Rate {
            param(
              [double]$Value
            )

            if ($Value -ge 1000000) {
              return ('{0}M' -f [Math]::Round($Value / 1000000, 2))
            }

            if ($Value -ge 1000) {
              return ('{0}K' -f [Math]::Round($Value / 1000, 2))
            }

            return [Math]::Round($Value, 2).ToString()
          }

          $baselineResults = Invoke-TimeBenchmarkSuite -Label 'master-msvc-pgo' -PhpExe $env:BASELINE_PHP -IniFile $env:BASELINE_INI -OutputFile (Join-Path $resultsDir 'master-msvc-pgo-time.json')
          $customResults = Invoke-TimeBenchmarkSuite -Label 'master-pr-21611-clang' -PhpExe $env:CUSTOM_PHP -IniFile $env:CUSTOM_INI -OutputFile (Join-Path $resultsDir 'master-pr-21611-clang-time.json')

          $baselineThroughputResults = @(
            (Invoke-PhpBenchMetric -Label 'master-msvc-pgo' -PhpBinary $env:BASELINE_PHPBENCH_BIN -PhpConfig $env:BASELINE_PHPBENCH_OFF_CONFIG -BenchmarkName 'phpbench-no-jit' -OutputFile (Join-Path $resultsDir 'master-msvc-pgo-phpbench-no-jit.json') -XmlFile (Join-Path $resultsDir 'master-msvc-pgo-phpbench-no-jit.xml')),
            (Invoke-PhpBenchMetric -Label 'master-msvc-pgo' -PhpBinary $env:BASELINE_PHPBENCH_BIN -PhpConfig $env:BASELINE_PHPBENCH_TRACING_CONFIG -BenchmarkName 'phpbench-jit' -OutputFile (Join-Path $resultsDir 'master-msvc-pgo-phpbench-jit.json') -XmlFile (Join-Path $resultsDir 'master-msvc-pgo-phpbench-jit.xml')),
            (Invoke-HttpRpsMetric -Label 'master-msvc-pgo' -PhpExe $env:BASELINE_PHP -IniFile $env:BASELINE_INI -Port 8091 -OutputFile (Join-Path $resultsDir 'master-msvc-pgo-http-rps.json'))
          )

          $customThroughputResults = @(
            (Invoke-PhpBenchMetric -Label 'master-pr-21611-clang' -PhpBinary $env:CUSTOM_PHPBENCH_BIN -PhpConfig $env:CUSTOM_PHPBENCH_OFF_CONFIG -BenchmarkName 'phpbench-no-jit' -OutputFile (Join-Path $resultsDir 'master-pr-21611-clang-phpbench-no-jit.json') -XmlFile (Join-Path $resultsDir 'master-pr-21611-clang-phpbench-no-jit.xml')),
            (Invoke-PhpBenchMetric -Label 'master-pr-21611-clang' -PhpBinary $env:CUSTOM_PHPBENCH_BIN -PhpConfig $env:CUSTOM_PHPBENCH_TRACING_CONFIG -BenchmarkName 'phpbench-jit' -OutputFile (Join-Path $resultsDir 'master-pr-21611-clang-phpbench-jit.json') -XmlFile (Join-Path $resultsDir 'master-pr-21611-clang-phpbench-jit.xml')),
            (Invoke-HttpRpsMetric -Label 'master-pr-21611-clang' -PhpExe $env:CUSTOM_PHP -IniFile $env:CUSTOM_INI -Port 8092 -OutputFile (Join-Path $resultsDir 'master-pr-21611-clang-http-rps.json'))
          )

          $baselineMeta = & $env:BASELINE_PHP -n -r "echo PHP_VERSION, '|', PHP_ZTS ? 'TS' : 'NTS';"
          $customMeta = & $env:CUSTOM_PHP -n -r "echo PHP_VERSION, '|', PHP_ZTS ? 'TS' : 'NTS', '|', ZEND_VM_KIND;"

          $baselineSource = if ([string]::IsNullOrWhiteSpace($env:BASELINE_ARTIFACT_RUN_ID)) { 'current run' } else { $env:BASELINE_ARTIFACT_RUN_ID }
          $clangSource = if ([string]::IsNullOrWhiteSpace($env:CLANG_ARTIFACT_RUN_ID)) { 'current run' } else { $env:CLANG_ARTIFACT_RUN_ID }

          $summaryLines = @(
            '# PHP master Windows benchmark',
            '',
            ('- Baseline: {0}' -f $baselineMeta),
            ('- Custom: {0}' -f $customMeta),
            ('- Baseline artifact source: {0}' -f $baselineSource),
            ('- Clang artifact source: {0}' -f $clangSource),
            ('- Iterations per benchmark: {0}' -f $iterations),
            ('- PHPBench iterations: {0}' -f $phpBenchIterations),
            ('- HTTP throughput benchmark: built-in server with k6 ({0} VUs for {1})' -f $env:K6_VUS, $env:K6_DURATION),
            '',
            '## Time-based benchmarks (lower is better)',
            '',
            '| Benchmark | Baseline avg (ms) | PR avg (ms) | Delta | Speedup |',
            '| --- | ---: | ---: | ---: | ---: |'
          )

          foreach ($baselineResult in $baselineResults) {
            $customResult = $customResults | Where-Object benchmark -EQ $baselineResult.benchmark

            if ($null -eq $customResult) {
              throw ('Missing custom result for {0}' -f $baselineResult.benchmark)
            }

            $delta = [Math]::Round((($baselineResult.avg_ms - $customResult.avg_ms) / $baselineResult.avg_ms) * 100, 2)
            $speedup = [Math]::Round($baselineResult.avg_ms / $customResult.avg_ms, 2)
            $deltaLabel = if ($delta -ge 0) { ('+{0}%' -f $delta) } else { ('{0}%' -f $delta) }
            $summaryRow = '| {0} | {1} | {2} | {3} | {4}x |' -f $baselineResult.benchmark, $baselineResult.avg_ms, $customResult.avg_ms, $deltaLabel, $speedup

            $summaryLines += $summaryRow
          }

          $summaryLines += @(
            '',
            '## Throughput benchmarks (higher is better)',
            '',
            '| Benchmark | Baseline | PR | Delta | Speedup |',
            '| --- | ---: | ---: | ---: | ---: |'
          )

          foreach ($baselineResult in $baselineThroughputResults) {
            $customResult = $customThroughputResults | Where-Object benchmark -EQ $baselineResult.benchmark

            if ($null -eq $customResult) {
              throw ('Missing custom throughput result for {0}' -f $baselineResult.benchmark)
            }

            $delta = [Math]::Round((($customResult.value - $baselineResult.value) / $baselineResult.value) * 100, 2)
            $speedup = [Math]::Round($customResult.value / $baselineResult.value, 2)
            $deltaLabel = if ($delta -ge 0) { ('+{0}%' -f $delta) } else { ('{0}%' -f $delta) }
            $summaryRow = '| {0} | {1} {2} | {3} {4} | {5} | {6}x |' -f $baselineResult.benchmark, (Format-Rate -Value $baselineResult.value), $baselineResult.unit, (Format-Rate -Value $customResult.value), $customResult.unit, $deltaLabel, $speedup

            $summaryLines += $summaryRow
          }

          Set-Content -Path $env:GITHUB_STEP_SUMMARY -Value $summaryLines

      - name: Upload benchmark results
        uses: actions/upload-artifact@v4
        with:
          name: php-master-pr-21611-benchmarks
          path: ${{ runner.temp }}/benchmark-results
          if-no-files-found: error
