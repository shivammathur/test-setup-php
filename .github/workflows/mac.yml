name: Test workflow #test cache after fix
on: [push]
jobs:
  run:    
    runs-on: ${{ matrix.operating-system }}
    strategy:
      fail-fast: false
      matrix:      
        operating-system: [macos-latest]
        php-versions: ['5.3', '5.4', '5.5']
    name: PHP ${{ matrix.php-versions }} Test on ${{ matrix.operating-system }}
    env:
      extensions: apcu, igbinary, html_parse
      key: cache-darwin1
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: install dependency
      run: brew install ekhtml
    - name: Setup cache environment
      id: cache-env
      uses: shivammathur/cache-extensions@develop
      with:
        php-version: ${{ matrix.php-versions }}
        extensions: ${{ env.extensions }}
        key: ${{ env.key }}

    - name: Cache extensions
      uses: actions/cache@v1
      with:
        path: ${{ steps.cache-env.outputs.dir }}
        key: ${{ steps.cache-env.outputs.key }}
        restore-keys: ${{ steps.cache-env.outputs.key }}
        
    - name: Verify cache
      run: ls ${{ steps.cache-env.outputs.dir }}

    - name: Setup PHP
      uses: shivammathur/setup-php@develop
      with:
        php-version: ${{ matrix.php-versions }}
        extensions: ${{ env.extensions }}
    - name: Testing PHP version
      run: |
        php -v        
    - name: Testing
      run: |
        ls -la $(php -i | grep -Ei "extension_dir => /usr" | sed -e "s|.*=> s*||")
        php -m
        php -r "echo phpversion('apcu');"       
