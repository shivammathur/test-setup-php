# OpenSSL 4 Windows Artifact Checks

This orphan branch downloads the merged artifacts from
`shivammathur/php-windows-builder` run `28561349625` and runs focused OpenSSL
userland checks against each PHP 8.6 VS18 Windows runtime zip.
